The application follows a responsive design, so resizing the browser (or changing the device 
through developer options) will show how it looks on movile devices. I have also removed the 
left half of the page containing the blue and white images for mobile devices as it was 
looking very untidy beside the registration form.